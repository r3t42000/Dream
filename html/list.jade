<!DOCTYPE html>
html(lang="en")
head
	meta(charset="UTF-8")
	title Document
	link(rel="stylesheet", href="../css/reset.css")
	link(rel="stylesheet", href="../css/base.css.css")
	link(rel="stylesheet", href="../css/layout.css")
	link(rel="stylesheet", href="../css/list.css")
body
	.wrapper#list
		//- /* navbar   */
		header#overlay
			.container
				.navbar.u-cf
					a(href="/dreamplatform").logo
						img(src="../img/dream_logo.svg", alt="")
					a.menu_toggle#overlay
						span
					.navbar-nav.popup_nav
						ul.nav_menu
							li.menu_item
								a(href="/dreamplatform/html/dreamStory.html") 夢境內容
							li.menu_item
								a(href="/dreamplatform/html/knowledge.html") 夢境小知識
							li.menu_item.login
								a 登入
					a.botton.nav__botton(href="/dreamplatform/html/list.html") 立即分享
		//- /* $List   */
		.section
		.title 夢境故事分享
		.list
			.list_title 夢境資訊
			.list_box
				label  日期
				.selectText.year 2017 年
				select(v-model="thisMonth")
					option(selected="") 請選擇
					option(v-for="i in 12") {{i}}
				.selectText 月

				.selectText
				select(v-model="thisDay")
					option(selected="") 請選擇
					option(v-for="i in 31") {{i}}
				.selectText 日

				label 睡覺時刻
				select(v-model="sleepHour")
					option(selected="") 請選擇
					option(v-for="i in 24") {{i}}
				.selectText 點
				select(v-model="sleepMinutes")
					option(selected="") 請選擇
					option(v-for="i in 4") {{(i-1)*15}}
				.selectText 分
				label 起床時刻
				select(v-model="timeHour")
					option(selected="") 請選擇
					option(v-for="i in 24") {{i}}
				.selectText 點
				select(v-model="timeMinutes")
					option(selected="") 請選擇
					option(v-for="i in 4") {{(i-1)*15}}
				.selectText 分
				label 夢境風格
				.classify
					.class(v-for="(c,c_id) in classdata" ,v-on:click="style_click(c_id,classNumber)",v-bind:class="c_id==classNumber?'clicking':''") {{c.name}}
					input.chosen_class(v-if="classNumber==3",placeholder="其他風格" v-model="otherClass")
				label 情緒
				.classify.moodbox
					.class.mood(v-for="(p,p_id) in perspective" ,v-on:click="perspective_click(p_id)",:class="p.class==true?'clicking':''") {{p.name}}
					input.chosen_class(placeholder="還有什麼情緒呢 ?")(v-if="perspective[7].class==true",v-model="mood_other")
				br
				br
			.list_title 夢境故事
			.list_box
				label 標題
				input.input_title(v-model="story_title")
				label 內文
				textarea.article(v-model="story_text")(placeholder="夢境內容")
				label 是否有以下的特殊狀態
				.slect_lis
					.class(v-for="(l,l_id) in special_Lists",@click="special_click(l_id)",v-bind:class="l.class==true?'clicking':''"
					) {{l.name}}
					input.chosen_class(placeholder="還有什麼特殊狀態呢 ?")(v-if="special_Lists[3].class==true",v-model="special_other")
		h4.send 送出
	script(src="../js/jquery-3.2.1.min.js")
	script(src="../js/vue.js")
	script(src="../js/list.js")